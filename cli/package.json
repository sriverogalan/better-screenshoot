{
  "name": "@better-screenshoot/cli",
  "version": "0.5.1",
  "private": true,
  "scripts": {
    "build": "cargo build --release -p better-screenshoot-cli",
    "test": "cargo test -p capture-core"
  }
}
